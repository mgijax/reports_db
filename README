
Edits for Sybase-to-Postgres support:

1) lib_py_postgres

2) lib_py_report

3) to add:

try:
    if os.environ['DB_TYPE'] == 'postgres':
	import pg_db
	db = pg_db
        db.setTrace()
	db.setAutoTranslate(False)
        db.setAutoTranslateBE()
    else:
        import db
except:
    import db

4) the lindon/postgres version will also need a 'imsr' schema copy
ALL_CellLine_GeneTrap.py
ALL_CellLine_Targeted.py
ALL_CRE.py
MGI_Knockout.py
MGI_Recombinase_Full.py
**done**

5) issues

mgimarkerfeed
	mainly translations

